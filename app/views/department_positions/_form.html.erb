<%= form_with(model: department_position, local: true) do |form| %>
  <% if department_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(department_position.errors.count, "error") %> prohibited this department_position from being saved:</h2>

      <ul>
      <% department_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :department_id %>
    <%= form.select :department_id, options_for_select(Department.all.map { |category| [category.name, category.id] }) %> 
  </div>

  <div class="field">
    <%= form.label :position_id %>
    <%= form.select :position_id, options_for_select(Position.all.map { |category| [category.name, category.id] }) %> 
  </div>

  <div class="field">
    <%= form.label :decription %>
    <%= form.text_area :decription, id: :department_position_decription %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
