<%= form_with(model: employment_type, local: true) do |form| %>
  <% if employment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employment_type.errors.count, "error") %> prohibited this employment_type from being saved:</h2>

      <ul>
      <% employment_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :employment_type_name %>
  </div>

  <div class="field">
    <%= form.label :decription %>
    <%= form.text_area :decription, id: :employment_type_decription %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
